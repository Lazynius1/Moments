# Obsoleto — usar archivos separados (mismo contenido que antes en este fichero):
#
#   firestore.rules           → service cloud.firestore (antes líneas 1–1667 de rules.txt)
#   storage.rules             → Storage alineado con StoragePathBuilder + legado
#   storage.rules.permissive  → snapshot consola anterior (auth = todo el bucket)
#
# Deploy:
#   firebase deploy --only firestore:rules
#   firebase deploy --only storage
